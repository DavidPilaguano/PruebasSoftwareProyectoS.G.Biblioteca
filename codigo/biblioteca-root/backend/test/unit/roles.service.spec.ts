import { Test, TestingModule } from '@nestjs/testing';
import { RolesService } from '../../src/roles/roles.service';

// TODO: Implementar pruebas para el servicio de roles
// Pruebas esperadas:
// - crear nuevo rol con validación
// - validar nombre de rol único
// - obtener todos los roles
// - obtener rol por ID
// - actualizar información del rol
// - eliminar rol
// - prevenir eliminación de rol si está asignado a usuarios
// - obtener permisos asociados a un rol

/*
describe('RolesService', () => {
  let service: RolesService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [RolesService],
    }).compile();

    service = module.get<RolesService>(RolesService);
  });
});
*/